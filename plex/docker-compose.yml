version: "3"

volumes:
    plex_data: {}

services:
    plex:
        image: linuxserver/plex:version-1.20.3.3437-f1f08d65b
        container_name: plex
        hostname: plex
        restart: unless-stopped

        volumes:
            - plex_data:/config
            - /mnt/WCC7K0KE9F7K/music:/media/music
            - /mnt/WCC7K0KE9F7K/video:/media/video

        network_mode: host
        environment:
            - PUID=1000
            - PGID=1000
            - VERSION=docker
